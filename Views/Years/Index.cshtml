@model IEnumerable<SchoolPortal.Models.Year>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Teacher.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.YearNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.YearName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.YearNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.YearName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.YearId }) |
            @Html.ActionLink("Details", "Details", new { id=item.YearId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.YearId })
        </td>
    </tr>
}

</table>
